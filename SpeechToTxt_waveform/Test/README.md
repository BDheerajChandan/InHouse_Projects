
cd New_Folder
    cd Frontend : 
        Use command to create a application : npm create vite@latest audio_amplify -- --template react --yes
        : npm install react
        : npm install react-dom
        : npm install bootstrap
        : pnpm dev
        
    cd Backend :
        : python -m venv audio_waveform
        : audio_waveform\Scripts\activate
        : pip install -r requirements.txt
        : uvicorn app.main:app --reload

    cd Test :
        : python -m venv audio_Test
        : audio_Test\Scripts\activate
        : pip install -r requirements.txt
        : python test.py
=======================================================================================================================================

noice ancellation
whatsaap UI based graph
covert button for audio to text speech recog nition + Edit